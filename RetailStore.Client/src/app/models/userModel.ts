export interface userModel {
    email: string;
    displayName: string;
    token: string;
}

export interface addressModel {
    firstName: string;
    lastName: string;
    street: string;
    city: string;
    state: string;
    zipcode: string;
}